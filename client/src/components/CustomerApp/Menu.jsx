// Import dependencies
import React from 'react';

// Menu component
// Populated with menu specific to a restaurant
const Menu = () => (
  <div className="Menu DebugComponentRed">
    <p>This is the <strong>Menu</strong> component</p>
    <p>Fix it so it is rendered at <code>/restaurants/:restaurant_id/menu</code></p>
  </div>
);

export default Menu;
