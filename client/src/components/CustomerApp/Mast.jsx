// Import dependencies
import React from 'react';

// Import CSS
import '../../styles/Mast.css';

const Mast = (props) => (
  <div className="customer-mast">
    <span>{'Test'}</span>
  </div>
);

export default Mast;
