// Import dependencies
import React from 'react';

// OrderStatus component
const OrderStatus = () => (
  <div className="OrderStatus DebugComponentRed">
    <p>This is the <strong>OrderStatus</strong> component</p>
    <p>Fix it so it is rendered at <code>/orders/:order_id/status</code></p>
  </div>
);

export default OrderStatus;
