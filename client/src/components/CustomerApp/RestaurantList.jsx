// Import dependencies
import React from 'react';

// RestaurantList component
// A list of available restaurants
const RestaurantList = () => (
  <div className="RestaurantList DebugComponentRed">
    <p>This is the <strong>RestaurantList</strong> component</p>
    <p>Fix it so it is rendered at <code>/restaurants</code></p>
  </div>
);

export default RestaurantList;
